<div class=container-fluid>
<%= image_tag @artist.photo.image, class: "artist-img" %>
<h1 class="align-left"><%= @artist.name %></h1>

<p class="align-left"><%= @artist.bio %></p>
<ul class="inline">
  <li>
    <%= link_to artist_path(@artist), method: :delete, data: { confirm: "Are you sure?" } do %>
    <i class="fa fa-minus-circle" aria-hidden="true"></i> Delete Artist</li>
    <% end %>
</ul>
</div>

<div class="songs">
<ul>
<% @artist.songs.each do |song| %>
<li>
  <%= link_to song_path(song), method: :delete, data: { confirm: "Are you sure?" } do %>
  <i class="fa fa-trash" aria-hidden="true"></i>
  <% end %>
   <%= song.name %></li>
<% end %>
</ul>
<%= render "song_form" %>
</div>
